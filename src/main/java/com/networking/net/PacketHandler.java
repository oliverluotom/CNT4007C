package com.networking.net;

import com.networking.model.*;

/**
 * Contains faculties to handle packets.
 */
public class PacketHandler {

    private Peer peer;

    public PacketHandler(Peer peer) {
        this.peer = peer;
    }

    public void handlePacket(Packet p) {

    }
}
